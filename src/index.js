/**
 * Editor entry for future core block variations and styles.
 *
 * Aculect Blocks intentionally starts by extending core blocks rather than
 * registering custom blocks. Add custom blocks only after core blocks, block
 * styles, variations, patterns, and block supports are insufficient.
 */
